{
    "private": true,
    "scripts": {
        "watch": "cross-env NODE_ENV=development mix watch --mix-config=./webpack.mix",
        "dev": "cross-env NODE_ENV=development mix --mix-config=./webpack.mix",
        "prod": "cross-env NODE_ENV=production mix --production --mix-config=./webpack.mix"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.13",
        "cross-env": "^7.0.3",
        "laravel-mix": "^6.0.49",
        "postcss-nested": "^6.0.0"
    }
}
